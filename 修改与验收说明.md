# AERO GLASS / FLIGHT LAB · 修改与验收说明

个人作品集已基于原有区块结构升级，原始个人内容、联系方式、两个项目、教育与八张证书完整保留。

## 核心视觉变化

以原有航空发动机为首屏主体，用深航空背景、钛金属文字、冰蓝流线与少量航空橙建立层次。玻璃集中在悬浮导航、按钮、项目说明与联系 Dock；普通介绍、能力和教育区域使用留白与工程线框。浅色主题采用白钛实验室配色。

新增 LH 航空几何标记、HUD、1.76 秒内完成的首屏序列、标题遮罩、测试记录数据面板、大图项目机库、系统能力矩阵、证书档案和右侧章节进度。

## 修改文件

| 文件 | 修改内容 |
| --- | --- |
| index.html | 保留原 DOM 区块和内容；迁出内联样式和交互；添加 HUD、标记、无障碍与渐进增强结构 |
| assets/aviation.css | 统一设计变量、深浅色、玻璃材质、响应式与动效 |
| assets/aviation.js | 统一动效、导航、项目、证书、主题和复制逻辑 |
| README-打开说明.txt | 更新本地查看及上传方法 |
| assets/icons.css、Phosphor-Portfolio.woff2 | 新增原图标库的 17 个图标子集，实际加载约 4 KB；原库文件保留 |
| assets/Geist-Latin.woff2 | 新增约 29 KB 的本地 Geist 可变字体 |
| assets/Geist-OFL.txt、Phosphor-LICENSE.txt | 字体及图标许可 |
| README.md、.nojekyll | 新增维护与 GitHub Pages 部署说明 |

136 段原文核对没有遗漏（SELECTED WORK 作为栏目装饰标签改为 PROJECT HANGAR）。所有原图片与图标资源逐字节保留。技能百分数保留原自评值，显示为工程线段，而非传统填充进度条。

## 动效架构

代码位于一个 IIFE 中，没有全局变量污染，按模块分区：

- PointerController 只记录事件目标，MouseState 通过与帧间隔关联的 lerp 平滑更新；视差、高光、倾斜和气流共享坐标。
- AirflowCanvas 使用连续流线和局部速度扰动，光标停止后恢复。每 32 秒可出现一次淡航迹。章节观察器调整气流方向、网格强度与联系区收束。
- MotionController 统一管理 RAF、暂停、系统减少动态、页面可见性和本地存储。
- RevealController 使用一个 IntersectionObserver 处理 fade、slide、mask、stagger、number、label；标题内层使用遮罩，外层负责可靠的进入视口检测。
- CounterController 在进入视口后播放一次数字动画；原文与读屏名称保持稳定。
- NavigationController 管理章节状态、滚动后的导航形态、手机抽屉及焦点。
- ProjectTabs 提供 480ms 的过渡、键盘方向键、快速连续切换的取消处理。
- CertificateViewer 使用原生 dialog，支持平滑关闭、Esc、背景关闭与焦点归还。

## 手机与性能处理

小于 768px 或触控设备关闭鼠标环、3D 倾斜和鼠标视差。手机采用独立环境流动，Canvas 降为 8 条流线、约 30 次/秒；项目上下排列，证书两列，证书查看提示始终可见，成果栏横向滑动，导航使用玻璃抽屉。

桌面视觉计算上限约 60 次/秒；DPR 上限 2；高光上限约 30 次/秒且只更新当前元素；倾斜上限 2.5°；视差最多约 10px。帧内无 querySelector、getBoundingClientRect、流线数组或渐变重建。布局测量只在进入悬停或尺寸变化时进行。

暂停后 RAF 停止、Canvas 像素保持不变；项目切换和弹窗继续可用。页面隐藏停止环境调度。减少动态时关闭环境、视差、计数和复杂揭示，全部内容完整显示。Canvas、IntersectionObserver、存储与背景模糊均提供降级。

## 检查结果

测试日期：2026-09-05。浏览器：本机 Edge/Chromium 自动化。测试包括 1440px 桌面、390px 触控手机，以及 320、375、768、1024px 宽度检查；这些宽度均未发现横向页面溢出。

- 所有实际图片加载成功；本地 Geist 和 Phosphor 字体加载成功。
- 导航锚点、手机菜单、背景关闭、键盘焦点约束正常。
- 两个项目 Tab、快速连续切换、左右方向键正常。
- 证书点击、Enter、空格、Esc、背景关闭和焦点恢复正常。
- 电话与在线试玩链接保持原地址；邮箱、微信、QQ 复制均实际读回剪贴板核对。
- 暂停、刷新后记忆、系统减少动态、主题切换正常。
- Canvas、视口观察器、本地存储不可用的模拟降级通过；file:// 本地直接打开通过。
- 自动化检查未发现 JavaScript 异常或 console error。
- axe：深色、浅色、手机和手机菜单均未发现所扫描 WCAG A/AA 规则违规。自动化检查不等同于所有辅助设备的人工认证。

| Lighthouse | 桌面 | 模拟手机 |
| --- | ---: | ---: |
| 性能 | 100 | 96 |
| 无障碍 | 100 | 100 |
| 最佳实践 | 100 | 100 |
| SEO | 100 | 100 |
| 最大内容绘制 LCP | 0.4 s | 2.0 s |
| 总阻塞时间 TBT | 0 ms | 180 ms |
| 布局偏移 CLS | 0 | 0 |

以上为本地 Lighthouse 模拟结果，不能保证所有设备固定 60 FPS 或实际公网获得同样分数。未进行真机 iOS/Safari/Firefox 全覆盖测试；保留了这些浏览器所需的标准 API 与材质降级。源码保留可读性，未为消除工具提示而压缩源文件。

## GitHub Pages

可以继续直接部署。上传包中没有 npm 依赖、开发工具、备份或测试截图。解压后把 index.html、assets、.nojekyll、README.md 放到仓库根目录，再从 Settings → Pages 配置实际存放文件的分支和 /(root)。本次未访问你的 GitHub 账户，也未执行线上发布。

[GitHub 官方部署说明](https://docs.github.com/en/pages/getting-started-with-github-pages/configuring-a-publishing-source-for-your-github-pages-site)

动效参考链接本次读取失败；作品的布局、材质、颜色与交互均依据你提供的航空工程要求实现。
